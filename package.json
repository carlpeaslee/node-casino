{
  "name": "node-casino-2",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.23.0",
    "babel-preset-latest": "^6.22.0",
    "babel-preset-stage-2": "^6.22.0",
    "nodemon": "^1.11.0"
  },
  "scripts": {
    "build": "yarn run clean && babel src --out-dir lib --copy-files",
    "start": "nodemon src --exec babel-node src",
    "clean": "rm -r lib"
  },
  "babel": {
    "presets": [
      "latest",
      "stage-2"
    ]
  },
  "dependencies": {
    "express": "^4.14.1",
    "pokersolver": "^2.1.2",
    "redis": "^2.6.5",
    "socket.io": "^1.7.3",
    "socket.io-redis": "^4.0.0"
  }
}
